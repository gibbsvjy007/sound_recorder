enum AudioState {
  isPlaying,
  isPaused,
  isStopped,
  isRecording,
  isRecordingPaused,
}

enum Media {
  file,
  buffer,
  asset,
  stream,
  remoteExampleFile,
}

enum PlayState{
  isPlaying,
  isStopped
}